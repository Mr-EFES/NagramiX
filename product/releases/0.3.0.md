# NagramiX 0.3.0

Build `34936275726` produced the initial unsigned ARM64 IPA from NagramiX commit
`39df1acdad22dd5f73af373ea9923f36b5ccf04a`; it is superseded by the corrected
asset below. Both builds use pinned Telegram-iOS
`6ad963e5b62d354da79040f388ae2b9132fb17b8`. Device acceptance remains pending.

The release asset was replaced after build `34974992373` compiled the corrected
editable Forward Without Author composer path from commit
`f9855cedca033d85f0e3b0c45e2152583026fac4`. Its SHA-256 is
`7ff46e8ebca88c1e31282573019839a8bf66265e1e7c76aadbe5a38e0ed900ee`.

NagramiX 0.3.0 replaces the defective 0.2.8 test build, whose clean-install welcome carousel displayed raw `Tour.*` keys because Telegram's generated Russian application localization was empty.

## Corrections in this candidate

1. The overlay now creates a complete bundled Russian application dictionary from the pinned English dictionary and replaces all six welcome titles, all six descriptions and the start button with Russian values.
2. Telegram's Bazel resource list includes that real Russian dictionary instead of generating its normal empty `ru.lproj` placeholder.
3. CI validates every required Russian welcome key both immediately after applying the overlay and inside the natively built `.app`; missing, empty, raw-key or non-Cyrillic values fail the build.
4. The Russian primary action and native alternative iPhone-language action are retained, with a readable bundled fallback before the downloaded langpack is available.
5. The tinted dark clean-install theme and maximum-width channel-post implementation remain included for renewed device testing.
6. The previously accepted rear-camera round-video behavior remains unchanged.

## Validation status

The macOS/Xcode ARM64 build, source and built-app Russian localization validation, unsigned packaging, artifact validation and release publication passed for the corrected asset from exact source commit `f9855cedca033d85f0e3b0c45e2152583026fac4`. The unsigned IPA must be externally signed. No corrected runtime behavior is accepted until it is checked on a physical iPhone.
